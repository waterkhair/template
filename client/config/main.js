// Modules
const API = require('./api'),
    APP = require('./app'),
    ERRORS = require('./errors'),
    GULP = require('./gulp'),
    HAPI = require('./hapi'),
    NOTIFICATIONS = require('./notifications');

// Main configuration
module.exports = {
    API,
    APP,
    ERRORS,
    GULP,
    HAPI,
    NOTIFICATIONS
};
