// Modules
const API = require('./api');
const APP = require('./app');
const GULP = require('./gulp');
const HAPI = require('./hapi');

// Main configuration
module.exports = {
    API,
    APP,
    GULP,
    HAPI
};
